spring:
  application:
    name: real-time-incident-notification-service-test

pyroscope:
  server:
    address: http://test-pyroscope-server
  auth:
    user: test-user
    password: test-password

twilio:
  accountSID: abc123
  authToken: xyz987
  phoneNumber:

demo:
  phoneNumber: "+15555551212"

services:
  user-store:
    url: "http://localhost:8083"

